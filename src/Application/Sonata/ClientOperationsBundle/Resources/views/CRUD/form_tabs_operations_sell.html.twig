{% block form_tabs %}
<div class="clientoperations_tabs_types_block">
    <ul class="nav nav-pills">
        {% if client_id %}
        <li {% if active_tab=='v01tva' %}class="active"{% endif %}>
            <a href="{{ url('admin_sonata_clientoperations_v01tva_list', {'filter[client_id][value]':client_id}) }}">V01-TVA</a>
        </li>
        {% endif %}
    </ul>
</div>
{% endblock %}